// math.ts
/*
function sum(a: number, b: number) {
  return a + b;
}
//const summer = sum(10, 20); // Error: '10'은 number에 할당될 수 없습니다.
//console.log(summer);

var total = sum(10, 20);
total.toLocaleString();
*/

function sum(a: number, b: number): number {
  return a + b;
}
var total = sum(10, 20);
total.toLocaleString();